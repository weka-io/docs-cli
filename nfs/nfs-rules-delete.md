```
Usage:
nfs rules delete 

Description:
    Commands for deleting NFS-rules

Subcommands:
   dns   Delete a DNS rule from an NFS client group
   ip    Delete an IP rule from an NFS client group

```
