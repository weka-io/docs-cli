```
Usage:
nfs interface-group port 

Description:
    Commands that manage nfs interface-groups' ports

Subcommands:
   add      Add a host's port to an interface group
   delete   Add a host's port to an interface group

```
